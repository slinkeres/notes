# Notes — простое приложение для заметок 📝

## 🚩 Ключевые возможности

### ✅ ВЫПОЛНЕНО (Обязательные требования)
- Создание заметок с текстом (через `input`)  
  - Проверка на пустую строку.
- Редактирование заметки:
  - Двойной клик по тексту или кнопка ✏️ для перехода в режим редактирования.
  - Сохранение — Enter, отмена — Escape.
  - Кнопки "Сохранить" / "Отменить" в режиме редактирования.
- Сохранение между сеансами:
  - Использование localStorage.
  - Автосохранение при изменениях (через `useEffect`).
  - Загрузка при инициализации приложения.
- Первая заметка при запуске:
  - Если localStorage пуст, создаётся примерная заметка.

### ✅  ВЫПОЛНЕНО (Желательные требования)
- Несколько заметок (массив tasks`) с уникальными `id (через `Date.now()`).
- Вывод списка заметок (рендер через `map`) и состояние "нет заметок".
- Редактирование любой заметки (`editingId` для отслеживания).
- Удаление заметок (кнопка 🗑).
- Фильтрация массива.

### ⭐️ Дополнительные функции
- Отметка о выполнении (кнопка ✅/❌), класс completed для стилизации.
- Сортировка по статусу — выполненные задачи отображаются в конце.
- Визуальное выделение (зачеркивание выполненных).
- Удобный UX: добавление по Enter, отмена редактирования по Escape, автофокус при редактировании.

---

## 🎯 Быстрый старт

### Клонирование
```bash
git clone https://github.com/slinkeres/notes
cd notes
```
### Установка зависимостей
```bash
  npm install
```

### Запуск в режиме разработки
```bash
npm run dev
```
